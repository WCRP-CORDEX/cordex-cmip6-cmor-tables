name: cmor-tests
channels:
  - conda-forge
  - nodefaults
dependencies:
  - xarray
  - numpy
  - netCDF4
  - pytest
  - py-cordex
  - cf_xarray
  - cmor>=3.8.0
  - fsspec
  - requests
  - aiohttp
  - cfchecker
  - cdo
  - python-cdo
  - pint
  - pip:
    - git+https://github.com/euro-cordex/py-cordex.git # always use master for now
